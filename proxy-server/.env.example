# Robinhood — use EITHER username/password OR a static token (not both)
# Option 1: Auto-login with credentials (preferred — handles token refresh automatically)
RH_USERNAME=
RH_PASSWORD=
# Option 2: Pre-obtained OAuth token (use if MFA blocks auto-login)
RH_AUTH_TOKEN=

ANTHROPIC_API_KEY=
ALLOWED_ORIGIN=
PORT=3001
